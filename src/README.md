# Source

The project currently uses the root-level ESPHome YAML without custom source
components. Place future reusable C++, headers, or ESPHome external components
here and document how they are included.
