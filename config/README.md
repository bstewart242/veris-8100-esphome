# Configuration

Use this directory for non-secret supporting configuration that does not belong
in the primary ESPHome YAML. Keep machine-specific and credential-bearing files
out of version control.
